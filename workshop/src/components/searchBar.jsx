import React from 'react';

const SearchBar = () => {
  return (
    <input
      type="text"
      className="form-control
      form-search"
      placeholder=""
    />
  );
};

export default SearchBar;
